interface PageProps {
  params: {
    fileid: string;
  };
}

const Page = ({ params }: PageProps) => {
  // retrieve the file id

  // make database call

  return <div>Page</div>;
};

export default Page;
